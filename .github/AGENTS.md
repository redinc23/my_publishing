RULES:

1. NEVER create duplicate PRs for same task
2. If PR exists → update it
3. Always rebase on main before push
4. Fix CI before asking for merge
5. Close your own stale PRs

FLOW:
task → branch → PR → fix CI → done
